/*
  3 INSTRUCCIONES:
  1. Complete los condicionales con el correspondiente
     operador (>, <, ==, <=, >=)
*/

  let numero1 = 10;
  let numero2 = 20;
    if (numero1>numero2) {
      console.log("numero1 es mayor que numero2");
    }
  
    // MODIFIQUE LA SIGUIENTE CONDICION
    if (numero2 % 2==0) {
      console.log("numero2 es positivo");
    }
  
    // MODIFIQUE LA SIGUIENTE CONDICION
    if (numero2 % 2==1 || numero2 != 0) {
      console.log("numero2 es negativo o distinto de cero");
    }
  
    // MODIFIQUE LA SIGUIENTE CONDICION
    if (numero1 % 2 == 0 || numero1 != 0) {
      console.log("numero1 es positivo y distinto de cero");
    }

